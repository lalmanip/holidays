apiVersion: v1
kind: Service
metadata:
  name: vivance-holidays-api-service
  labels:
    app: vivance-holidays-api
spec:
  type: NodePort
  selector:
    app: vivance-holidays-api
  ports:
    - name: http
      port: 8095
      targetPort: 8095
      nodePort: 30095
